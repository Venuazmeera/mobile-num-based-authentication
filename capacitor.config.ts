import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.student',
  appName: 'school-corner',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
